---
interface Props {
    name: string;
    link: string;
}

const { name, link } = Astro.props as Props;
---

<a href={link} class="flex gap-2 px-6 py-3
    border-t-3 border-l-3 border-accent hover:border-surface
    text-text text-2xl
    transition-all duration-100"
>
    {name}
</a>
